// inference.js - placeholder for node inference
async function infer(prompt) {
  // Replace with real model call (local model or API)
  return `Mock response for: ${prompt}`;
}

module.exports = { infer };
